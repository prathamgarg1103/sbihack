# --- Web layer ---
fastapi>=0.115
uvicorn[standard]>=0.32   # local dev server (Vercel provides its own ASGI server)
pydantic>=2.9

# --- Agent core ---
anthropic>=0.40           # only imported when ANTHROPIC_API_KEY is set
python-dotenv>=1.0

# --- RAG / corpus parsing ---
# Pure-Python TF-IDF (engine/rag.py) — no scikit-learn/numpy needed.
PyYAML>=6.0
