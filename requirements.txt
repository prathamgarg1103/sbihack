# Vercel Python serverless function deps (api/index.py).
# Kept lean for serverless: pure-Python TF-IDF means no scikit-learn/numpy.
fastapi>=0.115
pydantic>=2.9
python-dotenv>=1.0
PyYAML>=6.0
anthropic>=0.40   # only imported when ANTHROPIC_API_KEY is set
